spring.application.name=SafePlate

# Use local profile by default
spring.profiles.active=local

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# TODO: AI ?? URL ?? ??
ai-server.url=http://localhost:8080
